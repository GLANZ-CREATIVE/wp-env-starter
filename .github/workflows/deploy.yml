# name: Deploy

# on:
#   push:
#     branches:
#       - main
#       - develop
#   workflow_dispatch:

# jobs:
#   deploy:
#     name: FTP Deploy
#     runs-on: ubuntu-latest
#     timeout-minutes: 15

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "22.18.0"
#           cache: "npm"

#       - name: Upgrade npm
#         run: npm install -g npm@11.4.1

#       - name: Install dependencies
#         run: npm ci --prefer-offline --no-audit

#       - name: Build site
#         run: npm run build
#         env:
#           NODE_ENV: production

#       - name: Set deployment variables
#         id: vars
#         run: |
#           if [ "${{ github.ref }}" == "refs/heads/main" ]; then
#             echo "environment=production" >> $GITHUB_OUTPUT
#             echo "ftp_host=${{ secrets.FTP_HOST_PROD }}" >> $GITHUB_OUTPUT
#             echo "ftp_user=${{ secrets.FTP_USER_PROD }}" >> $GITHUB_OUTPUT
#             echo "ftp_password=${{ secrets.FTP_PASSWORD_PROD }}" >> $GITHUB_OUTPUT
#             echo "server_dir=/glanz-web.jp/public_html/domain/wp-content/themes/theme-name/" >> $GITHUB_OUTPUT
#           else
#             echo "environment=staging" >> $GITHUB_OUTPUT
#             echo "ftp_host=${{ secrets.FTP_HOST_STG }}" >> $GITHUB_OUTPUT
#             echo "ftp_user=${{ secrets.FTP_USER_STG }}" >> $GITHUB_OUTPUT
#             echo "ftp_password=${{ secrets.FTP_PASSWORD_STG }}" >> $GITHUB_OUTPUT
#             echo "server_dir=/glanz-web.jp/public_html/stg.iapa/wp-content/themes/iapa/" >> $GITHUB_OUTPUT
#           fi

#       - name: FTP Deploy
#         uses: SamKirkland/FTP-Deploy-Action@4.3.0
#         with:
#           server: ${{ steps.vars.outputs.ftp_host }}
#           username: ${{ steps.vars.outputs.ftp_user }}
#           password: ${{ steps.vars.outputs.ftp_password }}
#           server-dir: ${{ steps.vars.outputs.server_dir }}
#           local-dir: theme/
#           exclude: |
#             node_modules
#             .git
#             .github
#             *.md
#             .gitignore
#             .cursorrules
#             package*.json
#             vite.config.js
#             postcss.config.js
#             eslint.config.mjs
#             lefthook.yml
#             setup.js
#             sql
#             uploads
