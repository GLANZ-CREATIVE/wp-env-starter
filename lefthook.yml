pre-commit:
  parallel: true
  commands:
    format:
      run: npm run format
    stylelint:
      run: npm run stylelint
    eslint:
      run: npm run eslint
    php-lint:
      run: npm run php:lint
